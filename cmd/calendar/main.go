// Command calendar runs the full pipeline: fetch the source calendar from
// freecalend, update the durable archive under data/, regenerate the live
// .ics, and hand the result off for upload.
package main

import "fmt"

func main() {
	fmt.Println("rin-tateishi-calendar: pipeline not yet implemented")
}
