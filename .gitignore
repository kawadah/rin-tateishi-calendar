.*
mise.local.lock
mise.local.toml

!.github/
!.vscode/
!.gitignore

# Claude Code
!.claude/
.claude/worktrees/
.claude/settings.local.json
CLAUDE.local.md

# Committed config that would otherwise be caught by the .* rule above
!.golangci.yml
!.env.example

# Build output
/dist/
/bin/

# Compiled command binaries (go build ./cmd/<x> emits ./<x>)
/calendar
/inspect-har
/verify-protocol
