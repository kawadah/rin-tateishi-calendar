# Fetches the source calendar, updates the durable archive under data/, and
# publishes the live .ics to Cloudflare R2.
#
# Required GitHub configuration (Settings -> Secrets and variables -> Actions):
#   Secrets:   CLOUDFLARE_ACCOUNT_ID (your Cloudflare account ID)
#              CLOUDFLARE_R2_ACCESS_KEY_ID      (R2 S3-compatible access key)
#              CLOUDFLARE_R2_SECRET_ACCESS_KEY  (R2 S3-compatible secret key)
#   Variables: R2_BUCKET              (e.g. rin-tateishi-calendar)
#              R2_OBJECT_KEY          (e.g. calendar.ics)
name: Publish calendar

on:
  schedule:
    # Every 6 hours at 00/06/12/18 JST (Asia/Tokyo has no DST).
    - cron: 0 */6 * * *
      timezone: Asia/Tokyo
  workflow_dispatch:

# Serialize runs so the archive commit-back never races another run.
concurrency:
  group: publish
  cancel-in-progress: false

permissions: {}

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write # to commit the updated data/ archive back
    steps:
      - uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        id: app-token
        with:
          client-id: ${{ vars.UPDATER_APP_CLIENT_ID }}
          private-key: ${{ secrets.UPDATER_APP_PRIVATE_KEY }}
          permission-contents: write

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: true
          token: ${{ steps.app-token.outputs.token }}

      - name: Set up toolchain (mise)
        uses: jdx/mise-action@7e36c90d9ab29c415a2384db3006f3ec8a8cc654 # v4.2.4
        with:
          install_args: go

      - name: Run pipeline # fetch -> archive -> ics
        run: mise run pipeline

      - name: Get GitHub App bot user ID
        id: updater-app-user-id
        run: echo "id=$(gh api "/users/${UPDATER_APP_SLUG}[bot]" --jq .id)" >> "$GITHUB_OUTPUT"
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          UPDATER_APP_SLUG: ${{ steps.app-token.outputs.app-slug }}

      - name: Commit archive changes
        run: |
          set -euo pipefail
          if [ -z "$(git status --porcelain data/)" ]; then
            echo "archive unchanged"
            exit 0
          fi
          
          git config user.name "${UPDATER_APP_SLUG}[bot]"
          git config user.email "${UPDATER_APP_USER_ID}+${UPDATER_APP_SLUG}[bot]@users.noreply.github.com"
          ts="$(TZ=Asia/Tokyo date +'%Y-%m-%d %H:%M')"

          git add data/
          git commit -m "Update schedule (${ts})" -m "[skip ci]"
          git push
        env:
          UPDATER_APP_SLUG: ${{ steps.app-token.outputs.app-slug }}
          UPDATER_APP_USER_ID: ${{ steps.updater-app-user-id.outputs.id }}

      - name: Upload .ics artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: calendar-ics
          path: dist/calendar.ics
          if-no-files-found: error

  # Upload runs with only read scope and no persisted git credentials, so the write-scoped GITHUB_TOKEN is never present while wrangler (and its deps) run.
  upload:
    needs: publish
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Download .ics artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: calendar-ics
          path: dist

      - name: Upload .ics to R2
        run: |
          set -euo pipefail
          : "${AWS_ACCESS_KEY_ID:?set secret CLOUDFLARE_R2_ACCESS_KEY_ID}"
          : "${AWS_SECRET_ACCESS_KEY:?set secret CLOUDFLARE_R2_SECRET_ACCESS_KEY}"
          : "${CLOUDFLARE_ACCOUNT_ID:?set secret CLOUDFLARE_ACCOUNT_ID}"
          : "${R2_BUCKET:?set repo variable R2_BUCKET}"
          : "${R2_OBJECT_KEY:?set repo variable R2_OBJECT_KEY}"

          endpoint="https://${CLOUDFLARE_ACCOUNT_ID}.r2.cloudflarestorage.com"
          target="s3://${R2_BUCKET}/${R2_OBJECT_KEY}"

          aws s3 cp dist/calendar.ics "$target" --endpoint-url "$endpoint" --cache-control "public, max-age=3600" --content-type "text/calendar; charset=utf-8"
          echo "uploaded ${target}"
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.CLOUDFLARE_R2_ACCESS_KEY_ID }}
          AWS_DEFAULT_REGION: auto
          AWS_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_SECRET_ACCESS_KEY }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          R2_BUCKET: ${{ vars.R2_BUCKET }}
          R2_OBJECT_KEY: ${{ vars.R2_OBJECT_KEY }}

  # Independent contract canary: detect protocol drift early. Failing here (or a
  # failed publish) emails the repo watchers via GitHub's default notifications.
  canary:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up toolchain (mise)
        uses: jdx/mise-action@7e36c90d9ab29c415a2384db3006f3ec8a8cc654 # v4.2.4
        with:
          install_args: go

      - name: Verify protocol
        run: mise run verify-protocol

      - name: Upload raw response on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: verify-protocol-failure
          path: verify-protocol-failure.json
          if-no-files-found: ignore
